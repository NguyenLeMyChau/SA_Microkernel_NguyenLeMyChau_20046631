plugins {
    id 'java'
}

group = 'org.example'
version = '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
    testImplementation platform('org.junit:junit-bom:5.9.1')
    testImplementation 'org.junit.jupiter:junit-jupiter'
    implementation project(':core') // Phụ thuộc vào module "core"
}

jar {
    archiveBaseName = 'JapaneseLanguage'
    version = '1.0-SNAPSHOT'
    destinationDirectory = file('build/libs')
}

test {
    useJUnitPlatform()
}